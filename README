This is a mirror of http://www.vim.org/scripts/script.php?script_id=4080

This script is intended to be used with autocmd bufwritepost when a mercurial commit is needed after each change in the file.
